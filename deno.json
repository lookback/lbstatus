{
  "tasks": {
    "run": "deno run --import-map ./import_map.json -c deno.json --allow-run --allow-read --allow-net main.ts",
    "compile": "deno compile --quiet --import-map ./import_map.json -c deno.json  --allow-run --allow-read --allow-net -o target/$ARCH/lbstatus main.ts"
  },
  "fmt": {
    "files": {
      "include": [
        "main.ts",
        "deps.ts"
      ]
    },
    "options": {
      "useTabs": false,
      "lineWidth": 100,
      "indentWidth": 4,
      "singleQuote": true,
      "proseWrap": "preserve"
    }
  }
}
